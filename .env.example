# ===============================================
# NOM Production Environment Configuration
# ===============================================
# Copy this file to .env and update with your production values

# ===============================================
# Database Configuration
# ===============================================
POSTGRES_DB=nom
POSTGRES_USER=nom
POSTGRES_PASSWORD=change_this_in_production
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ===============================================
# ===============================================

# ===============================================
# Application Configuration
# ===============================================
ASPNETCORE_ENVIRONMENT=Production
API_PORT=8080
UI_PORT=80
# Comma- or semicolon-separated list. Leave unset only in Development (CORS fails closed in Production).
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===============================================
# ===============================================

# ===============================================
# Security Configuration
# ===============================================

# ===============================================
# Public URL of the UI, used in account email links (confirmation, reset).
FRONTEND_URL=http://localhost:4210

# Feature Flags (Optional)
# ===============================================

# ===============================================
# Email Configuration (Optional)
# ===============================================
# Leave SMTP_HOST empty to use console-logging NoOp sender (for development)
# Leave empty to disable outbound email entirely (NoOp sender).
EMAIL_SMTP_HOST=
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=
EMAIL_SMTP_PASSWORD=
EMAIL_FROM_ADDRESS=noreply@nom.local
EMAIL_FROM_NAME=NOM
EMAIL_USE_SSL=true
